The code in this project has been generated over time by multiple LLMs.  Your task is to review and understand the entire codebase and then generate a detailed phased plan to improve the codebase.  The goal of the improvements are:

- The code is updated to the latest Angular version 21.x
- The code adheres to the latest Angular best practices
- Coding style and naming conventions are consistent across the codebase
- The code is idiomatic, concise, and does not employ esoteric "tricks"
- The project is well-structured
- The use of APIs and SSE events follow best practices - if an API or SSE needs to be updated to improve application, details those suggestions as prompts that can be provided to the backend developers
- Unneeded code and files are deleted - only needed code and files remain
- UI elements are consistent and provide an optimal experience for the end user
- Missing or unimplmented UI functions are listed and detailed so that future implementation plans can be made
- Update @Agents.md, @ai/angular_best_practices.md, and @ai/llmsrules.md to ensure that future LLMS understand exactly what is needed to properly evaluate and update the codebase

You will be in build mode but you are not to change any code. You will create and document a detailed, well-structured plan with distinct phases that future LLMs can use to implement the plan exactly as you define across multiple sessions.  Breaking changes are allowed - we must achieve the goals detailed above. Create an internal rubric that evaluates the plan and ensures that you present the best possible plan.  Do not assume your first approach is correct - use the rubric to evaluate the plan and present only the best and final approach as the plan.

Success criteria:

- All improvement goals are met
- The plan is detailed enough to be implemented exactly as intended with no ambiguity
- The code compiles, runs, and works as intended
- The user interface provides a modern and useful experience to the end user

Ask clarifying questions if needed.  If you do not know something or need additional information stop and ask.

Create a file in the ai/plans/ directory called claude_code_improvement_plan.md and write the final plan there.